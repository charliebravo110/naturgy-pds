# 📁 src/mobile-apps/carousel/what-is-new-data

This folder contains the data for the `what-is-new` carousel.

To change the content replace the images and fill the `whatIsNewData` array accordingly.

If the content is not ready you can temporarily disable the carousel by setting `enabled` to false in `whatIsNewData`.