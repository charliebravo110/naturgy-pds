# 📁 src/mobile-apps/carousel/welcome-data

This folder contains the data for the `welcome` (or onboarding) carousel.

To change the content replace the images and fill the `welcomeData` array accordingly.

If the content is not ready you can temporarily disable the carousel by setting `enabled` to false in `welcomeData`.