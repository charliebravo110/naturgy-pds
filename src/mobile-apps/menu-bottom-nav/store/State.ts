export default interface State {
  /** hamburguer menu, should be visible for current user type? (domestic, 900...) */
  showBurguer: boolean
  /** bottom nav menu, should be visible for current user type? (domestic, 900...) */
  showBottomNav: boolean
}
