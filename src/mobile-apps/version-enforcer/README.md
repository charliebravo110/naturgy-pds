# version-enforcer

If a new version is available, the user is forced to update, being redirected to `/version-enforcer` 

The redirection to `/version-enforcer` is done with the `<VersionEnforcer />` component from `App.tsx`.