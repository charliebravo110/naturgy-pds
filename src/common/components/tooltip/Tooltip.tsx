import React from 'react'

import Tooltip from '@material-ui/core/Tooltip'

const NaturgyTooltip = (props: any) => {
  return (
    <Tooltip {...props} />
  )
}

export default NaturgyTooltip
